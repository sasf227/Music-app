version: "3.9"

services:
  frontend:
    build: ./frontend
    container_name: music-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

  backend:
    build: ./backend
    container_name: music-backend
    ports:
      - "8070:8070"